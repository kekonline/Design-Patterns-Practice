const greet = (name) => `Hello, ${name}!`;

const greet = (name) => {
    const message = `Hello, ${name}!`;
    return message;
};

const greet = () => 'Hello!';

const add = (a, b) => a + b;

(() => {
    console.log('Hello!');
})();

const greet = async (name) => {
    const message = `Hello, ${name}!`;
    return message;
};